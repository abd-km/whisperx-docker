{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "whisperx-api.serviceAccountName" . }}
  labels:
    {{- include "whisperx-api.labels" . | nindent 4 }}
{{- end }}

